// In your MainActivity.kt, wrap your app content like this:

@Composable
fun YourAppContent() {
    AppProvider {
        // Your existing app navigation and content goes here
        // For example:
        val navController = rememberNavController()
        AppNavigation(
            navController = navController,
            tokenManager = yourTokenManager,
            startDestination = "login"
        )
    }
}